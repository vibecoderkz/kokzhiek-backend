# Database Configuration
# Required: PostgreSQL connection string from Neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# JWT Configuration
# Required: Secret key for JWT token signing (min 32 characters)
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long
JWT_EXPIRES_IN=7d

# Email Configuration (Optional - for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME="Your App Name"

# Frontend Configuration
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app,http://localhost:5173

# ========================================
# RENDER.COM DEPLOYMENT INSTRUCTIONS
# ========================================
#
# To deploy on Render.com, you MUST add these environment variables
# in the Render Dashboard (do NOT commit real values to git):
#
# 1. Go to https://dashboard.render.com
# 2. Select your kokzhiek-backend service
# 3. Click "Environment" tab
# 4. Add the following variables with YOUR actual values:
#
#    DATABASE_URL = <your Neon.tech connection string>
#    JWT_SECRET = <your secret key>
#    NODE_ENV = production
#    FRONTEND_URL = https://kokzhiek-new.vercel.app
#    ALLOWED_ORIGINS = https://kokzhiek-new.vercel.app,https://kokzhiek.icai.kz
#
# 5. Click "Save Changes" - Render will auto-redeploy
#
# Without DATABASE_URL, the service WILL NOT START!
